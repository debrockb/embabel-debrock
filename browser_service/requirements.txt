# M.A.T.O.E Browser Service — pinned for reproducible NAS/Portainer builds.
#
# Browser automation libraries break often across minor versions. These pins
# were chosen in April 2026 against a working browser-use integration. To
# upgrade, bump a single pin, rebuild the browser image, and re-test the
# /browse endpoint before promoting to production.
# browser-use 0.1.48 requires langchain-openai>=0.3.11 (per its install_requires).
# Pinning an older langchain-openai causes ResolutionImpossible at `pip install` —
# always bump these two together.
browser-use==0.1.48
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
langchain-anthropic==0.3.3
langchain-openai==0.3.11
python-dotenv==1.0.1
